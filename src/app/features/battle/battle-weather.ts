/**
 * Re-exported from the shared weather overlay so the Battle Arena and
 * Tournaments use one source of truth for weather data and picking.
 */
export * from '../../core/ui/weather-overlay/weather';
